# Minimal runtime for offline validation + dry-run honesty tests.
# Retrain profiles need the private lab (DVXR_LAB_ROOT) and its fuller env.
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2
scikit-learn>=1.3,<2
torch>=2.1,<3
pytest>=7.4,<9
